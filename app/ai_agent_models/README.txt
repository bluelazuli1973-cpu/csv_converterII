run the training model by the command in top level:
> python -m app.ai_agent_models.swedish_csv_to_training

test the model by the command in top level:
> python -m app.ai_agent_models.train_category_model

The model is now trained and can be used for prediction.





